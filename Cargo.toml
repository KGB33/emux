[package]
name = "emux"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
clap = { version = "4.6.1", features = ["derive", "cargo"] }
glob = "0.3.3"
grep = "0.4.1"
colored = "2"
mlua = { version = "0.11.6", features = ["lua54", "vendored"] }
